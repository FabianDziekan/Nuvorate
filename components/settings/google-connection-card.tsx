"use client";
import { useActionState, useTransition } from "react";
import { disconnectGoogleConnection, selectGoogleLocation } from "@/app/settings/actions";
export function GoogleConnectionCard({ configured, connection, message, locations = [] }: { configured: boolean; connection: { google_location_title: string | null; google_email: string | null } | null; message?: string; locations?: Array<{ locationName: string; locationTitle: string }> }) { const [pending, start] = useTransition(); const [selection, selectAction] = useActionState(selectGoogleLocation, { error: "" }); return <section className="mt-5 rounded-[24px] border border-black/[0.06] bg-white p-5 shadow-card sm:p-6"><h2 className="text-xl font-semibold">Połączenie z Google</h2>{!configured ? <p className="mt-3 text-sm text-black/45">Integracja Google nie została jeszcze skonfigurowana przez administratora.</p> : connection ? <div className="mt-4"><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">Połączono</span><p className="mt-4 font-semibold">{connection.google_location_title ?? "Lokalizacja Google"}</p><p className="mt-1 text-sm text-black/45">{connection.google_email}</p><p className="mt-4 text-sm text-black/45">Synchronizacja opinii zostanie włączona w kolejnym etapie.</p><div className="mt-5 flex flex-wrap gap-3"><a href="/api/google/connect" className="button-secondary">Połącz ponownie</a><button type="button" className="text-sm font-semibold text-red-600" disabled={pending} onClick={() => { if (window.confirm("Odłączyć Google? Nie usunie to danych NuvoRate.")) start(async () => { await disconnectGoogleConnection(); window.location.reload(); }); }}>Odłącz Google</button></div></div> : locations.length ? <form action={selectAction} className="mt-4"><p className="text-sm text-black/45">Wybierz lokalizację Google Business Profile.</p><select name="locationName" className="mt-4 h-11 w-full rounded-xl border border-black/10 bg-white px-3 text-sm">{locations.map((location) => <option key={location.locationName} value={location.locationName}>{location.locationTitle}</option>)}</select>{selection.error ? <p className="mt-2 text-sm text-red-600">{selection.error}</p> : null}<button className="button-primary mt-4">Połącz wybraną lokalizację</button></form> : <div className="mt-4"><p className="text-sm text-black/45">Połącz profil Google Business, aby w kolejnym etapie synchronizować opinie.</p>{message ? <p className="mt-3 text-sm font-medium text-black/55">{message}</p> : null}<a href="/api/google/connect" className="button-primary mt-5">Połącz z Google</a></div>}</section>; }
